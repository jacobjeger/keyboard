package com.megalife.ime.navigation

enum class DPadMode {
    /** D-pad navigates the app behind the keyboard */
    APP_NAV,
    /** D-pad navigates keys on the on-screen keyboard */
    KEYBOARD_NAV
}
